# Environment Variables Example
# Copy this file to .env.local and fill in your actual values

# MongoDB Connection String
# Get this from MongoDB Atlas: Database > Connect > Connect your application
# Replace <password> with your actual MongoDB Atlas password
# Replace <cluster-url> with your actual cluster URL
MONGODB_URI=mongodb+srv://promptix_admin:<password>@<cluster-url>/promptix_db?retryWrites=true&w=majority

# Example (DO NOT USE THIS - Replace with your actual connection string):
# MONGODB_URI=mongodb+srv://promptix_admin:mySecurePassword123@cluster0.abc123.mongodb.net/promptix_db?retryWrites=true&w=majority

# Instructions:
# 1. Copy this file and rename it to .env.local
# 2. Replace <password> with your MongoDB Atlas database user password
# 3. Replace <cluster-url> with your MongoDB Atlas cluster URL
# 4. Save the file
# 5. Never commit .env.local to git (it's already in .gitignore)
